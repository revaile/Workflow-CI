name: RandomForest_Advance_CI

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path: {type: str, default: "titanicdataset_preprocessing/train_preprocessed.csv"}
    command: "python modelling.py --data_path {data_path}"
